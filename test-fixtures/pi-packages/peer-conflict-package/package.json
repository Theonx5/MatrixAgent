{
  "name": "@pideck-fixtures/peer-conflict-package",
  "version": "1.0.0",
  "private": true,
  "description": "Local package fixture with peerDependencies used to exercise DefaultPackageManager remove under peer conflicts (0.80.7 regression). Install/remove only via SDK; no public registry.",
  "peerDependencies": {
    "react": "^18.0.0",
    "react-dom": "^19.0.0"
  },
  "peerDependenciesMeta": {
    "react": { "optional": true },
    "react-dom": { "optional": true }
  },
  "pi": {
    "extensions": ["./extensions/peer-ext.ts"]
  }
}
